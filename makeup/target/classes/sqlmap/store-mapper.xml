<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store.mapper">

<select id="list" resultType="store.StoreVO">
	select id, readcnt, no, title, writer, filename, writedate
	from store
</select>

<select id="detail" resultType="store.StoreVO">
	select id, readcnt, title, content, writer, filename, writedate
	from store
	where id=#{id}
</select>
</mapper>